{
  "name": "@mono-react/root",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start:server": "lerna exec --scope @mono-react/server -- yarn watch:dev",
    "start:client": "lerna exec --scope @mono-react/client -- yarn start:dev",
    "build:server": "lerna exec --scope @mono-react/server -- yarn build",
    "build:client": "lerna exec --scope @mono-react/client -- yarn build"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^4.0.0",
    "npm-run-all": "4.1.5"
  }
}
